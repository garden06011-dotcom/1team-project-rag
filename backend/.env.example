# OpenAI API 키 (필수)
# https://platform.openai.com/api-keys 에서 발급받으세요
OPENAI_API_KEY=sk-your-actual-api-key-here

# RAG 설정
# Distance 임계값 (0.65 = 유사도 67.5% 이상)
# ChromaDB의 cosine distance: 0(완전 유사) ~ 2(완전 다름)
# 낮을수록 더 유사한 문서만 반환 (기본값: 0.65)
RAG_SCORE_THRESHOLD=0.65

# 검색할 문서 개수 (기본값: 3)
RAG_TOP_K=3

# OpenAI 모델 이름 (기본값: gpt-4o-mini)
# 사용 가능한 모델: gpt-4o-mini, gpt-4o, gpt-3.5-turbo 등
RAG_MODEL_NAME=gpt-4o-mini

# LLM 창의성 (0~2, 기본값: 0.7)
# 0에 가까울수록 일관된 답변, 2에 가까울수록 창의적인 답변
RAG_TEMPERATURE=0.7

# 최대 응답 토큰 수 (기본값: 1000)
RAG_MAX_TOKENS=1000
